const Loading = () => {
    return (
        <div className="flex min-h-screen justify-center item">
            <span className="loading loading-bars loading-xl"></span>
        </div>
    );
};

export default Loading;